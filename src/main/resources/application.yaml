ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080

    jwt:
        secretKey: "my-very-secure-secret-key-12345"
        audience: "my-ktor-app"
        issuer: "my-ktor-server"